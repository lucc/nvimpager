#!/bin/sh

# If we are not on a tty just "be" cat.
if [ ! -t 1 ]; then
  exec cat "$@"
fi

RUNTIME=.
# TODO see what option less provides

if [ $# -eq 0 ]; then
  set -- -
fi

nvim \
  -u NONE \
  --cmd "set rtp+=$RUNTIME" \
  --cmd 'call pager#start()' \
  -c 'call pager#start2()' \
  "$@"
